# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database ---
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pgvector==0.3.4

# --- Validation / settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Auth / security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# --- Background jobs ---
celery==5.4.0
redis==5.0.8

# --- AI providers ---
# All three are optional at runtime: services/ai_extraction.py and
# services/embeddings.py import them lazily and fall back cleanly, so the
# app runs with any one provider configured, or none at all.
google-genai==2.23.0
anthropic==0.34.2
voyageai==0.2.3

# --- Object storage ---
boto3==1.35.24

# --- Utilities ---
# 0.0.18+ required: earlier versions have an unauthenticated DoS
# (CVE-2024-53981) reachable on any multipart endpoint, because the body is
# parsed before authentication runs.
python-multipart==0.0.18
httpx==0.27.2
tenacity==9.0.0

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database ---
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pgvector==0.3.4

# --- Validation / settings ---
# pydantic 2.12.5+ and httpx 0.28.1+ are floors set by google-genai. Do not
# lower them without also removing that package — a clean `pip install -r`
# will fail outright (ResolutionImpossible) rather than degrade, which is
# how the mismatch first showed up: as a failed deploy, not a warning.
pydantic==2.12.5
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Auth / security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# --- Background jobs ---
celery==5.4.0
redis==5.0.8

# --- AI providers ---
# All three are optional at runtime: services/ai_extraction.py and
# services/embeddings.py import them lazily and fall back cleanly, so the
# app runs with any one provider configured, or none at all.
google-genai==2.23.0
anthropic==0.34.2
voyageai==0.2.3

# --- Object storage ---
boto3==1.35.24

# --- Utilities ---
# 0.0.18+ required: earlier versions have an unauthenticated DoS
# (CVE-2024-53981) reachable on any multipart endpoint, because the body is
# parsed before authentication runs.
python-multipart==0.0.18
httpx==0.28.1  # floor set by google-genai; see the pydantic note above
tenacity==9.0.0

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
